package resolver

// This file will be automatically regenerated based on the schema, any resolver
// implementations
// will be copied through when generating and any unknown code will be moved to the end.
// Code generated by github.com/99designs/gqlgen version v0.17.90

import (
	"context"
	"fyp/graph"
	"fyp/graph/graphErrs"
	"fyp/graph/model"
	"fyp/internal/contexts"
	"strconv"
)

// CreateBooking creates a pending online booking for the authenticated user.
func (r *mutationResolver) CreateBooking(ctx context.Context, slotOptionID string) (*model.Booking, error) {
	currentUser, err := contexts.CurrentUser(ctx)
	if err != nil {
		return nil, graphErrs.ToGraphQLError(err)
	}
	optID, err := parseID(slotOptionID)
	if err != nil {
		return nil, graphErrs.ToGraphQLError(err)
	}
	b, err := r.App.BookingService.CreateBooking(ctx, currentUser.UserID, optID)
	if err != nil {
		return nil, graphErrs.ToGraphQLError(err)
	}
	status := model.BookingStatus(b.Status)
	return &model.Booking{
		BookingID:    strconv.FormatInt(b.BookingID, 10),
		UserID:       strconv.FormatInt(b.UserID, 10),
		SlotOptionID: strconv.FormatInt(b.SlotOptionID, 10),
		Status:       &status,
		BookingType:  model.BookingType(b.BookingType),
		SlotOption:   &model.ServiceSlotOption{},
		User:         &model.User{},
	}, nil
}

// PublicBusinesses returns all businesses for public browsing.
func (r *queryResolver) PublicBusinesses(ctx context.Context) ([]*model.BusinessProfile, error) {
	businesses, err := r.App.BusinessService.GetBusinesses(ctx, "")
	if err != nil {
		return nil, graphErrs.ToGraphQLError(err)
	}
	result := make([]*model.BusinessProfile, len(businesses))
	for i := range businesses {
		result[i] = graph.MapBusinessProfile(&businesses[i], nil)
	}
	return result, nil
}

// PublicServices returns services with options for a given business.
func (r *queryResolver) PublicServices(ctx context.Context, businessID string) ([]*model.Service, error) {
	bid, err := parseID(businessID)
	if err != nil {
		return nil, graphErrs.ToGraphQLError(err)
	}
	services, err := r.App.ServiceService.GetServicesByBusinessID(ctx, bid)
	if err != nil {
		return nil, graphErrs.ToGraphQLError(err)
	}
	result := make([]*model.Service, len(services))
	for i := range services {
		result[i] = graph.MapService(&services[i])
	}
	return result, nil
}

// PublicStaff returns active staff for a business (for customer slot filtering).
func (r *queryResolver) PublicStaff(ctx context.Context, businessID string) ([]*model.Staff, error) {
	bid, err := parseID(businessID)
	if err != nil {
		return nil, graphErrs.ToGraphQLError(err)
	}
	staffList, err := r.App.StaffService.GetStaffByBusinessID(ctx, bid)
	if err != nil {
		return nil, graphErrs.ToGraphQLError(err)
	}
	result := make([]*model.Staff, len(staffList))
	for i := range staffList {
		result[i] = graph.MapStaff(&staffList[i])
	}
	return result, nil
}

// AvailableSlots returns slots with no active booking for a given service option and date.
func (r *queryResolver) AvailableSlots(ctx context.Context, businessID string, serviceOptionID string, date string, staffID *string) ([]*model.ServiceSlot, error) {
	bid, err := parseID(businessID)
	if err != nil {
		return nil, graphErrs.ToGraphQLError(err)
	}
	optID, err := parseID(serviceOptionID)
	if err != nil {
		return nil, graphErrs.ToGraphQLError(err)
	}
	var sid *int64
	if staffID != nil && *staffID != "" {
		v, err := parseID(*staffID)
		if err != nil {
			return nil, graphErrs.ToGraphQLError(err)
		}
		sid = &v
	}
	slots, err := r.App.BookingService.GetAvailableSlots(ctx, bid, optID, date, sid)
	if err != nil {
		return nil, graphErrs.ToGraphQLError(err)
	}
	result := make([]*model.ServiceSlot, len(slots))
	for i := range slots {
		result[i] = graph.MapServiceSlot(&slots[i])
	}
	return result, nil
}

// RecentlyBookedBusinesses returns the businesses the current user recently booked.
func (r *queryResolver) RecentlyBookedBusinesses(ctx context.Context) ([]*model.BusinessProfile, error) {
	currentUser, err := contexts.CurrentUser(ctx)
	if err != nil {
		return nil, graphErrs.ToGraphQLError(err)
	}
	businesses, err := r.App.BookingService.GetRecentlyBookedBusinesses(ctx, currentUser.UserID)
	if err != nil {
		return nil, graphErrs.ToGraphQLError(err)
	}
	result := make([]*model.BusinessProfile, len(businesses))
	for i := range businesses {
		result[i] = graph.MapBusinessProfile(&businesses[i], nil)
	}
	return result, nil
}

// Mutation returns graph.MutationResolver implementation.
func (r *Resolver) Mutation() graph.MutationResolver { return &mutationResolver{r} }

// Query returns graph.QueryResolver implementation.
func (r *Resolver) Query() graph.QueryResolver { return &queryResolver{r} }

type mutationResolver struct{ *Resolver }
type queryResolver struct{ *Resolver }
